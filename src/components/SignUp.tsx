import React, { ReactElement } from 'react'

interface Props {
  
}

export default function SignUp({}: Props): ReactElement {
  return (
      <div id="signup-page">
        <input type="text"/>
      </div>
  )
}
